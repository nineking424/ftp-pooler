apiVersion: v1
kind: Secret
metadata:
  name: ftp-pooler-connections
  labels:
    app: ftp-pooler
type: Opaque
stringData:
  connections.ini: |
    # Example connections - replace with actual values
    [remote-server-a]
    type = ftp
    host = ftp.example.com
    port = 21
    user = username
    pass = changeme
    passive = true

    [local]
    type = local
    base_path = /data/storage
